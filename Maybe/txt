                /*
                let selected = content.querySelector(".htmlizeAnchorsReplaceElement").parentElement
                let keys=Object.keys(key);
                keys.forEach((e)=>{
                    selected[e]=key[e]
                })
                */

            /*
            const effect=()=>{
                let before=selected;
                let pattern=key;
                let node=document.createTextNode(before);
                const update=()=>{
                    if(before!=pattern()){
                            let result=func()
                            node.nodeValue=result;
                            before=result;
                    }
                }
                details.nodeLists.push(update)
                //globalEffect
                core.effectNodes.push(update)
                return node;
            }*/