    /*experimental*/
    const isMounted=()=>{
        /*
        let s=document.querySelector("."+details.key)
        return s?(s.isConnected?true:false):false
        */
        return FEC.isConnected
    }